
func x() {
    return func() { return "hello world from anonymous function" }
}

// x() returns an anonymous function
print x()

// The anonymous function can be assigned to any variable
y = x()

// Now 'y' can be used like a function, ie. using the call operator '()'
print y()

// We can also assign create anonymous functions and assign them to variables directly
z = func() { return "z is another anonymous function" }
print z()

print func(a) { return a * 5 }
