
// Simple factorial function
func factorial(n) {
    if n == 0 {
        return 1
    }
    return n * factorial(n - 1)
}

// Ask user for their choice
choice = int(input("Enter a number, see the factorial: "))
// Show the user their answer
print factorial(choice)
